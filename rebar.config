{erl_opts, [debug_info,
           {d, 'DEBUG', true},
           {src_dirs, ["src"]}
]}.

{deps, [

]}.

{cover_enable, true}.
{eunit_opts, [verbose, {report, {eunit_surefire, [{dir, "./eunit"}]}}]}.
